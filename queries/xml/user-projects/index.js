const archived = require('./archived');
const reviewed = require('./reviewed');
const toReview = require('./to-review');
const upcoming = require('./upcoming');

module.exports = {
  archived,
  reviewed,
  toReview,
  upcoming,
};
